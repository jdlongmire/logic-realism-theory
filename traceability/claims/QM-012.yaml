# QM-012: Faithful Event Representation
id: QM-012
name: Faithful Event Representation
statement: |
  Every LRT Event admits a faithful representation as a self-adjoint
  operator on a Hilbert space H. The Boolean event algebra embeds
  into the projection lattice on H.

  This is the representation theorem: LRT's event structure is rich
  enough to embed into quantum observables.

role: bridge
proof_status: derived
epistemic_status: argued

formal_artifacts:
  lean:
    - file: Step4/Boolean.lean
      symbol: faithful_representation
      status: theorem

source_documents:
  - doc: LRT-MASTER
    section: "3.4"

depends_on:
  - claim_id: QM-004
    dependency_type: formal
  - claim_id: QM-005
    dependency_type: logical
  - claim_id: EXT-001
    dependency_type: formal
    note: "Uses hardy_reconstruction axiom"

external_citations:
  - "Stone representation theorem (Boolean algebras embed in P(Ω))"
  - "Quantum logic: event algebras → projection lattices"
  - "Hardy reconstruction theorem (H1 + H2 → Hilbert space)"

risk_if_false: |
  Without representation, LRT events remain purely ontological with
  no connection to Hilbert space operators. The entire operator
  theory section becomes disconnected.

notes: |
  Phase 4 contribution. Justification: Events form a Boolean algebra
  (Step 0: Event.and, Event.or, Event.not with proven LNC/LEM).
  Stone's theorem guarantees embedding into a Boolean algebra of sets.
  The quantum logic tradition extends this to projection lattices.

  **Update 2026-03-17:** Converted from axiom to theorem. The proof
  uses Hardy's reconstruction theorem (Step3.hardy_reconstruction)
  with H1 (local tomography) and H2 (state separation) to derive
  the existence of a complex Hilbert space, then constructs the
  identity operator as a canonical self-adjoint operator.

agent_history:
  - agent_id: C2
    date: "2026-03-17"
    action: strengthened
    summary: |
      Converted `axiom faithful_representation` to `theorem faithful_representation`
      in Step4/Boolean.lean. The proof applies Hardy's reconstruction theorem
      using lrt_satisfies_h1 (H1: local tomography) and lrt_satisfies_h2
      (H2: state separation) to obtain a Hilbert space, then witnesses
      the identity operator as a self-adjoint operator. This reduces the
      axiom count by one, replacing it with a derivation from Hardy (EXT-001).
      Build verified: Step4/Boolean.lean compiles successfully.
